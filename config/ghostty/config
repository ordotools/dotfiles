# --- Appearance ---
font-family = "JetBrainsMono Nerd Font"
font-size = 14.5
# +5% line height-ish: use cell height adjust (accepts % or px)
adjust-cell-height = 5%
window-padding-x = 12
window-padding-y = 10

# mac translucency + blur
background = #141415
# I am just not feeling the transparency, unless we can first get neovim transparent
# background-opacity = 0.90
# background-opacity-cells = true
# background-blur = 20

# Built-in theme name (see: ghostty +list-themes)
# theme = tokyo-night

# Cursor & selection
cursor-style = bar
cursor-style-blink = true
cursor-opacity = 0.9
selection-foreground = "#1e1e2e"
selection-background = "#b4befe"

# Split cosmetics
split-divider-color = "#3a3a55"
unfocused-split-opacity = 0.80
unfocused-split-fill = "#000000"

# --- Shell & behavior ---
command = zsh
scrollback-limit = 200000

# mac niceties
macos-titlebar-style = tabs
macos-window-shadow = true
macos-option-as-alt = true

# ---------- Shaders (glow/CRT) ----------
# Place shaders in config/ghostty/shaders (repo path) -> ~/.config/ghostty/shaders (linked).
# custom-shader = ~/.config/ghostty/shaders/amber-crt.glsl
# custom-shader-animation = true
# Keep your .glsl files in ~/.config/ghostty/shaders
# custom-shader = ~/.config/ghostty/ghostty-shaders/mnoise.glsl

# custom-shader = ~/.config/ghostty/ghostty-shaders/bloom.glsl
# custom-shader-animation = true

# --- Keybinds (valid actions) ---
# Splits
keybind = cmd+d=new_split:right
keybind = cmd+shift+d=new_split:down
keybind = cmd+w=close_surface

# Tabs
keybind = cmd+t=new_tab
keybind = cmd+shift+[=previous_tab
keybind = cmd+shift+]=next_tab

# # Font zoom (works on US layout)
# keybind = cmd+shift+= increase_font_size
# keybind = cmd+-       decrease_font_size
# keybind = cmd+0       reset_font_size
#
# # Font zoom (Ghostty key names)
# keybind = cmd+shift+equal:increase_font_size
# keybind = cmd+minus:decrease_font_size
# keybind = cmd+digit0:reset_font_size

# # Font zoom
# keybind = cmd+shift+equal [increase_font_size]
# keybind = cmd+minus       [decrease_font_size]
# keybind = cmd+digit0      [reset_font_size]
#
# Zoom (exact Ghostty syntax)
keybind = cmd+shift+equal=increase_font_size:1
keybind = cmd+minus=decrease_font_size:1
keybind = cmd+0=reset_font_size

# Handy utilities
keybind = cmd+,=open_config
keybind = cmd+shift+r=reload_config
keybind = cmd+p=toggle_command_palette

