- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # ~/.vim: vim
    ~/.zshrc:
    ~/.gitconfig:

# - create:
#     - ~/downloads
#     - ~/.vim/undo-history

- shell:
    - 
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
