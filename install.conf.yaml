- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # ~/.vim: vim
    ~/.zshrc:
    ~/.gitconfig:

# - create:
#     - ~/downloads
#     - ~/.vim/undo-history

- shell:
#   - [git submodule update --init --recursive, Installing submodules]
  - echo "hey you guys"
  - sleep 2
  - 
    command: ./test.zsh
    stdout: true
    stderr: true
